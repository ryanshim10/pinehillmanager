FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn sqlite3 pandas gspread

COPY bridge.py .

EXPOSE 8000

CMD ["uvicorn", "bridge:app", "--host", "0.0.0.0", "--port", "8000"]
